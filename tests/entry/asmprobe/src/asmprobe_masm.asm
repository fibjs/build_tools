; The assembly of this library is MASM syntax (the ASM_MASM language), which is
; what the Windows toolchain of this repository assembles.  The value is data,
; so no instruction set is involved.

.DATA

PUBLIC asmprobe_asm_value

asmprobe_asm_value DD 41

END
