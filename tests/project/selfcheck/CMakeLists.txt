cmake_minimum_required(VERSION 3.10)

# Static library built through cmake/Library.cmake (target name = directory
# name, sources globbed by cmake/option_src.cmake).
include(${CMAKE_CURRENT_LIST_DIR}/../../../cmake/Library.cmake)
